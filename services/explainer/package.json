{
  "name": "@lfc/explainer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Writes a short German explanation for each verdict via an LLM and publishes it after the verdict (brief 5, 6a; ADR 0009).",
  "files": [
    "dist",
    "prompts",
    "!dist/**/*.test.*"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch --conditions=development src/main.ts",
    "start": "node dist/main.js",
    "test:unit": "vitest run --project unit",
    "test:int": "vitest run --project int"
  },
  "dependencies": {
    "@lfc/contracts": "workspace:*",
    "@lfc/providers": "workspace:*",
    "@lfc/service-kit": "workspace:*",
    "ioredis": "6.0.0",
    "prom-client": "15.1.3",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@testcontainers/redis": "12.1.0",
    "@types/node": "24.13.6",
    "@vitest/coverage-v8": "5.0.1",
    "testcontainers": "12.1.0",
    "tsx": "4.23.14",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
