$ make secrets-init SECRETS_DIR=<tmp>   # first run
created  <tmp>/redis_password (empty, fill in yourself)
created  <tmp>/searxng_secret (empty, fill in yourself)
created  <tmp>/gateway_token (empty, fill in yourself)
created  <tmp>/anthropic_api_key (empty, fill in yourself)
created  <tmp>/deepgram_api_key (empty, fill in yourself)
created  <tmp>/assemblyai_api_key (empty, fill in yourself)
created  <tmp>/brave_api_key (empty, fill in yourself)
created  <tmp>/tavily_api_key (empty, fill in yourself)
$ make secrets-init SECRETS_DIR=<tmp>   # second run, gateway_token already filled
kept     <tmp>/redis_password
kept     <tmp>/searxng_secret
kept     <tmp>/gateway_token
kept     <tmp>/anthropic_api_key
kept     <tmp>/deepgram_api_key
kept     <tmp>/assemblyai_api_key
kept     <tmp>/brave_api_key
kept     <tmp>/tavily_api_key
$ stat -f "%Lp %N" <tmp> <tmp>/*
700 <tmp>
600 <tmp>/anthropic_api_key
600 <tmp>/assemblyai_api_key
600 <tmp>/brave_api_key
600 <tmp>/deepgram_api_key
600 <tmp>/gateway_token
600 <tmp>/redis_password
600 <tmp>/searxng_secret
600 <tmp>/tavily_api_key
gateway_token still non-empty: yes
$ make secrets-init SECRETS_DIR=./secrets   # inside repo
refusing: SECRETS_DIR must be outside the repository (<repo>/secrets)
make: *** [secrets-init] Error 1
exit code: 2
