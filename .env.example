# Non-secret configuration for `docker compose`. Copy to `.env` and adjust.
# Secrets never go here: they live one file per secret in SECRETS_DIR (see docs/SECURITY.md).

# Directory with the secret files, outside the repository. Created by `make secrets-init`.
SECRETS_DIR=${HOME}/.config/live-factcheck/secrets
